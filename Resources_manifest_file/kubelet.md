# Allow manually install kubelet in worker node
## Download the installer
```
wget https://storage.googleapis.com/kubernetes-release/release/v1.13.0/bin/linux/amd64/kubelet
```
ps -aux | grep kubelet
